.cache/
.state/
dashboard.config.json
/data.json
.venv/
__pycache__/
*.pyc
web/node_modules/
web/dist/

# `/data.json` above is the retired builder's output path at the dashboard root --
# the live service's own scratch, never tracked content.
